import styled from 'styled-components';

const PhotoControlsStyled = styled.div`
  text-align: center;
`;

const PhotoControlButtonStyled = styled.button`
  background-color: rgb(6 182 212);
  border-radius: 0.25rem;
  border: 0;
  color: #fff;
  font-size: 18px;
  font-family: 'Montserrat', sans-serif;
  margin: 1rem 0.5rem 0;
  padding: 0.7rem 0.5rem;
  width: 10rem;
`;

export { PhotoControlsStyled, PhotoControlButtonStyled };
